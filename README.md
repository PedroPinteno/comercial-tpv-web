# Comercial TPV Web (Astro)

- Autenticación: Supabase Auth (email + password)
- Llamadas a Edge Functions vía `supabase.functions.invoke()`
- Páginas:
  - /login
  - /app (grid de categorías)
  - /app/search (buscador por categoría)

Scripts:
- `npm install`
- `npm run dev`
